version: '3.4'

services:


  # JOB STAT CONSUMER
  job-stat-consumer:
    network_mode: "host"
    image: seyfa/ktr:consumer-api
    # ports:
    #   - 8090:8080

  # CONSUMER FRONT
  job-stat-front:
    network_mode: "host"
    image: seyfa/ktr:consumer-front
